# TencentDB-Agent-Memory 新平台适配需求规格

## 一、适配要做什么

无论接入什么平台，适配的本质一句话：**监听平台的对话生命周期事件，从中提取 TdaiCore 需要的信息，调用对应的 TdaiCore 方法。**

适配层不参与记忆存储、不参与搜索算法、不参与 LLM 调用逻辑——这些事情 TdaiCore 内部已经全部完成。适配层只做一个事情：**翻译**——把平台特有的事件/参数格式翻译成 TdaiCore 的统一接口。

---

## 二、TdaiCore 能力清单（适配层需要调用的全部方法）

| 序号 | TdaiCore 方法 | 作用 | 调用时机 |
|:---:|--------------|------|---------|
| 1 | `handleBeforeRecall(userText, sessionKey)` | 召回 L1/L2/L3 相关记忆，返回注入 prompt 的文本 | Agent **收到用户消息后、开始回复前** |
| 2 | `handleTurnCommitted(turn)` | 记录本轮对话到 L0，并通知管线调度器 | Agent **完成一轮回复后** |
| 3 | `handleSessionEnd(sessionKey)` | 冲刷当前会话的缓存数据到持久化 | 会话**关闭/断开时** |
| 4 | `searchMemories({ query, limit, type, scene })` | 搜索 L1 结构化记忆 | Agent **主动调用记忆搜索工具时** |
| 5 | `searchConversations({ query, limit, sessionKey })` | 搜索 L0 原始对话记录 | Agent **主动调用对话搜索工具时** |
| 6 | `initialize()` | 初始化存储引擎、向量服务、管线调度器 | 平台**启动/插件加载时** |
| 7 | `destroy()` | 冲刷管线、关闭数据库连接、释放资源 | 平台**关闭/插件卸载时** |

---

## 三、核心适配表：事件 → 参数 → TdaiCore 调用

### 3.1 记忆召回（对话前）

| 项目 | 说明 |
|------|------|
| **触发平台事件** | Agent 收到用户消息后、构建 prompt 之前<br>• OpenClaw: `before_prompt_build`<br>• Hermes: `prefetch(query)` |
| **从平台获取的参数** | `userText` — 用户原始输入文本<br>`sessionKey` — 当前会话标识 |
| **调用 TdaiCore** | `handleBeforeRecall(userText, sessionKey)` |
| **TdaiCore 返回** | `{ prependContext, appendSystemContext }`<br>• `prependContext`: 相关 L1 记忆（注入用户 prompt 前面）<br>• `appendSystemContext`: L2 场景 + L3 画像 + 工具指南（注入系统 prompt 末尾） |
| **适配层要做的** | 把返回的文本注入到 Agent 的 prompt 中。不同平台注入方式不同：<br>• 有的平台直接拼到系统 prompt 末尾<br>• 有的平台通过 hook 返回值自动注入 |

**参照 OpenClaw：**

```typescript
// index.ts 第 529 行
api.on("before_prompt_build", async (event, ctx) => {
  const userText = event.prompt;          // ← 从事件取用户文本
  const sessionKey = ctx.sessionKey;      // ← 从上下文取会话 key
  const result = await core.handleBeforeRecall(userText, sessionKey);
  return result;  // ← OpenClaw 自动把返回值注入 prompt
});
```

**参照 Hermes（通过 Gateway）：**

```typescript
// server.ts 第 371 行
private async handleRecall(req, res) {
  const body = await parseJsonBody(req);
  // body.query = "用户文本"  ← 从 HTTP 请求体取
  // body.session_key = "..." ← 从 HTTP 请求体取
  const result = await this.core.handleBeforeRecall(body.query, body.session_key);
  sendJson(res, 200, { context: result.appendSystemContext });
  // ← 通过 HTTP 响应返回给 Hermes
}
```

---

### 3.2 记忆捕获（对话后）

| 项目 | 说明 |
|------|------|
| **触发平台事件** | Agent 完成一轮回复后<br>• OpenClaw: `agent_end`<br>• Hermes: `sync_turn(user_content, assistant_content)` |
| **从平台获取的参数** | `userText` — 用户原始输入文本<br>`assistantText` — Agent 本轮回复文本<br>`messages` — 本轮全部消息（含 tool_call、tool_result 等）<br>`sessionKey` — 当前会话标识<br>`sessionId` — 可选的子会话 ID |
| **调用 TdaiCore** | `handleTurnCommitted({ userText, assistantText, messages, sessionKey, sessionId })` |
| **TdaiCore 返回** | `{ l0RecordedCount, schedulerNotified }`<br>• `l0RecordedCount`: 写入 L0 的消息条数<br>• `schedulerNotified`: 是否触发了 L1/L2/L3 管线 |
| **适配层要做的** | 收集本轮对话的完整上下文传给 TdaiCore。注意：<br>• `messages` 数组必须包含所有角色（user/assistant/tool_call/tool_result）<br>• `userText` 应该是用户的**原始文本**（不含已注入的记忆） |

**参照 OpenClaw：**

```typescript
// index.ts 第 656 行
api.on("agent_end", async (event, ctx) => {
  // 从缓存取用户的原始文本（防止被 prependContext 污染）
  const originalUserText = pendingOriginalPrompts.get(ctx.sessionKey)?.text;
  
  await core.handleTurnCommitted({
    userText: originalUserText ?? "",
    messages: (event.messages as unknown[]) ?? [],  // ← 本轮全部消息
    sessionKey: ctx.sessionKey,
    sessionId: ctx.sessionId || undefined,
  });
});
```

**参照 Hermes：**

```typescript
// server.ts 第 393 行
private async handleCapture(req, res) {
  const body = await parseJsonBody(req);
  // body.user_content = "用户说的话"       ← HTTP 请求体
  // body.assistant_content = "AI的回复"    ← HTTP 请求体
  // body.session_key = "..."              ← HTTP 请求体
  await this.core.handleTurnCommitted({
    userText: body.user_content,
    messages: [
      { role: "user", content: body.user_content },
      { role: "assistant", content: body.assistant_content },
    ],
    sessionKey: body.session_key,
  });
}
```

---

### 3.3 会话结束

| 项目 | 说明 |
|------|------|
| **触发平台事件** | 会话关闭/断开时<br>• OpenClaw: 无独立事件，在 `gateway_stop` 中统一处理<br>• Hermes: `post_session_sync(session_key)` → `POST /session/end` |
| **从平台获取的参数** | `sessionKey` — 会话标识 |
| **调用 TdaiCore** | `handleSessionEnd(sessionKey)` |
| **TdaiCore 返回** | 无返回值 |
| **适配层要做的** | 在会话结束时通知 TdaiCore 冲刷该会话的缓存数据 |

---

### 3.4 Agent 主动搜索记忆 — tdai_memory_search

| 项目 | 说明 |
|------|------|
| **触发平台事件** | Agent 主动调用记忆搜索工具时<br>• OpenClaw: `api.registerTool("tdai_memory_search")`<br>• Hermes: `handle_tool_call("memory_tencentdb_memory_search")` |
| **从平台获取的参数** | `query` — 搜索关键词（**必填**）<br>`limit` — 返回条数（默认 5，最大 20）<br>`type` — 记忆类型过滤（可选：persona/episodic/instruction）<br>`scene` — 场景名过滤（可选） |
| **调用 TdaiCore** | `searchMemories({ query, limit, type, scene })` |
| **TdaiCore 返回** | `{ text, total, strategy }`<br>• `text`: 格式化后的搜索结果文本<br>• `total`: 搜索结果条数<br>• `strategy`: 使用的搜索策略（keyword/embedding/hybrid） |
| **适配层要做的** | 把工具调用参数原样传给 TdaiCore，把返回的 `text` 作为工具结果返回给 Agent |

**参照 OpenClaw：**

```typescript
// index.ts 第 352 行
api.registerTool({
  name: "tdai_memory_search",
  parameters: {
    type: "object",
    properties: {
      query: { type: "string", description: "搜索查询" },
      limit: { type: "number", description: "最多返回条数" },
      type:  { type: "string", enum: ["persona", "episodic", "instruction"] },
      scene: { type: "string", description: "按场景名过滤" },
    },
    required: ["query"],
  },
  async execute(_toolCallId, params) {
    const result = await core.searchMemories({
      query: String(params.query),
      limit: Number(params.limit) || 5,
      type: params.type,
      scene: params.scene,
    });
    // 返回给 Agent
    return { content: [{ type: "text", text: result.text }] };
  },
});
```

---

### 3.5 Agent 主动搜索对话 — tdai_conversation_search

| 项目 | 说明 |
|------|------|
| **触发平台事件** | Agent 主动调用对话搜索工具时 |
| **从平台获取的参数** | `query` — 搜索关键词（**必填**）<br>`limit` — 返回条数（默认 5，最大 20）<br>`sessionKey` — 按会话过滤（可选） |
| **调用 TdaiCore** | `searchConversations({ query, limit, sessionKey })` |
| **TdaiCore 返回** | `{ text, total }`<br>• `text`: 格式化后的搜索结果文本<br>• `total`: 搜索结果条数 |
| **适配层要做的** | 同 3.4，参数原样传递，结果为文本格式 |

---

### 3.6 初始化（平台启动时）

| 项目 | 说明 |
|------|------|
| **触发平台事件** | 平台启动 / 插件加载时 |
| **从平台获取的参数** | 无（但需要提前准备好 `HostAdapter` 实例和配置文件） |
| **调用 TdaiCore** | `new TdaiCore({ hostAdapter, config }).initialize()` |
| **TdaiCore 初始化内容** | 创建数据目录、初始化 SQLite/TCVDB 连接、启动向量嵌入服务、恢复管线调度器状态 |
| **适配层要做的** | 在调用 `initialize()` 之前，必须完成：<br>① 实现并创建 `HostAdapter` 实例<br>② 解析平台配置文件生成 `MemoryTdaiConfig` |

**参照两个平台：**

```typescript
// OpenClaw: index.ts 第 256-274 行
const hostAdapter = new OpenClawHostAdapter({ api, pluginDataDir, openclawConfig });
const cfg = parseConfig(api.pluginConfig);
const core = new TdaiCore({ hostAdapter, config: cfg });
await core.initialize();

// Gateway: server.ts 第 126-148 行
const adapter = new StandaloneHostAdapter({ dataDir, llmConfig, logger });
const cfg = loadGatewayConfig().memory;
const core = new TdaiCore({ hostAdapter: adapter, config: cfg });
await core.initialize();
```

**注意：** `HostAdapter` 必须实现 `src/core/types.ts` 中的 3 个方法：

```
getLogger()            → 提供日志实例
getRuntimeContext()    → 提供用户/会话上下文（userId, sessionId, sessionKey, dataDir 等）
getLLMRunnerFactory()  → 提供 LLM 调用能力（工厂模式创建 LLMRunner）
```

---

### 3.7 销毁（平台关闭时）

| 项目 | 说明 |
|------|------|
| **触发平台事件** | 平台关闭 / 插件卸载 / Gateway 进程退出时 |
| **从平台获取的参数** | 无 |
| **调用 TdaiCore** | `core.destroy()` |
| **TdaiCore 销毁内容** | 冲洗管线中缓存的未处理数据、关闭数据库连接、关闭嵌入服务 |
| **适配层要做的** | 确保在平台退出前调用此方法。注意：<br>• `destroy()` 是异步的，需要 `await`<br>• 建议设置超时（OpenClaw 设了 3s），防止无限等待 |

```typescript
// 两个平台的写法完全一样
await core.destroy();
```

---

## 四、HostAdapter 接口要求

无论用什么方式接入，适配层必须向 TdaiCore 提供一个实现了以下接口的 `HostAdapter`：

```
                        向 TdaiCore 暴露这 3 个能力
                        ─────────────────────

  getLogger() ────────────────►  TdaiCore 自己写日志
      返回 Logger 对象              (debug/info/warn/error)

  getRuntimeContext() ─────────►  TdaiCore 知道自己运行在哪个平台、
      返回 RuntimeContext 对象       数据存哪里、当前用户是谁

  getLLMRunnerFactory() ──────►  TdaiCore 在需要调用 LLM 时创建 Runner
      返回 LLMRunnerFactory 实例   (L1 提取/L1 去重/L2 场景/L3 画像都需要 LLM)
```

**这两种信息适配层必须从平台获取：**

| 字段 | 说明 | 从哪获取 |
|------|------|---------|
| `userId` | 用户标识 | 平台的多用户系统 |
| `sessionId` | 子会话 ID | 平台的会话管理 |
| `sessionKey` | 跨重连稳定的会话 key | 平台的会话管理 |
| `platform` | 平台标识字符串 | 固定填写，如 `"dify"`, `"cline"` |
| `dataDir` | 记忆数据存储目录 | 平台的数据目录 API |
| `workspaceDir` | 工作目录 | 平台的工具沙箱路径 |

---

## 五、LLMRunner 接口要求

TdaiCore 在一些离线任务中需要自己调用 LLM（不依赖 Agent）：

| 任务 | 功能 | 对 LLM 的要求 | 来源模块 |
|------|------|-------------|---------|
| L1 记忆提取 | 从对话中提取关键事实 | 纯文本输入输出（不需要工具） | `L1Extractor` |
| L1 去重合并 | 把重复记忆合并成一条 | 纯文本输入输出（不需要工具） | `L1Dedup` |
| L2 场景提取 | 将 L1 记忆归类到场景 | 需要文件工具（read/write/replace_in_file） | `SceneExtractor` |
| L3 画像生成 | 从场景总结用户画像 | 需要文件工具 | `PersonaGenerator` |

适配层必须提供一个 `LLMRunner` 实现，满足以下要求：

```
LLMRunner.run({ prompt, systemPrompt?, taskId, enableTools? }): Promise<string>
                                              ↑
                                    决定是否暴露文件工具给 LLM
                                    • false → L1 提取/去重
                                    • true  → L2 场景/L3 画像
```

**两种实现参考：**

| 方式 | 适用场景 | 参考代码 |
|------|---------|---------|
| 复用平台的 LLM 机制 | 平台是 Node.js，有内嵌 Agent | `OpenClawLLMRunner` — 调用 `api.runtime.agent.runEmbeddedPiAgent()` |
| 独立调用 LLM API | 平台无法复用，或需要独立配置 | `StandaloneLLMRunner` — 用 Vercel AI SDK 直连 OpenAI API |

---

## 六、完整适配检查清单

接入一个新平台，按以下清单逐项完成：

```
□ 1. 解析平台配置，生成 MemoryTdaiConfig 对象
     → 参考 src/config.ts 的 parseConfig()

□ 2. 实现 HostAdapter（3 个方法）
     □ getLogger() — 适配平台的日志系统
     □ getRuntimeContext() — 从平台 API 获取用户/会话/数据目录信息
     □ getLLMRunnerFactory() — 创建一个能调 LLM 的工厂

□ 3. 创建 TdaiCore 实例并调用 initialize()
     const core = new TdaiCore({ hostAdapter: adapter, config: cfg });
     await core.initialize();

□ 4. 注册对话前钩子 → handleBeforeRecall()
     监听 Agent 收到用户消息的事件
     提取 userText + sessionKey
     把返回的 prependContext/appendSystemContext 注入 Agent prompt

□ 5. 注册对话后钩子 → handleTurnCommitted()
     监听 Agent 完成回复的事件
     提取 userText + messages + sessionKey
     调用 handleTurnCommitted()

□ 6. 注册 Agent 工具 → searchMemories() + searchConversations()
     注册 tdai_memory_search 和 tdai_conversation_search
     参数原样传递，结果文本返回给 Agent

□ 7. 注册关闭钩子 → destroy()
     监听平台退出/插件卸载的事件
     调用 core.destroy()（异步，建议加超时）

□ 8. (可选) 会话结束通知 → handleSessionEnd()
     如果平台有独立的"会话关闭"事件，在此时调用
```

---

## 七、现有两个适配器的对照表（供参考）

| TdaiCore 方法 | OpenClaw 平台事件 / API | Hermes 平台事件 / API |
|--------------|------------------------|----------------------|
| `handleBeforeRecall()` | `api.on("before_prompt_build")` → 从 `event.prompt` + `ctx.sessionKey` 取参 | `provider.prefetch(query)` → POST `/recall` → 从 HTTP body 取 `query` + `session_key` |
| `handleTurnCommitted()` | `api.on("agent_end")` → 从 `event.messages` + `ctx.sessionKey` 取参 | `provider.sync_turn(user, assistant)` → POST `/capture` → 从 HTTP body 取 `user_content` + `assistant_content` + `session_key` |
| `searchMemories()` | `api.registerTool("tdai_memory_search")` → `execute(params)` | `provider.handle_tool_call("memory_tencentdb_memory_search")` → POST `/search/memories` |
| `searchConversations()` | `api.registerTool("tdai_conversation_search")` → `execute(params)` | `provider.handle_tool_call("memory_tencentdb_conversation_search")` → POST `/search/conversations` |
| `handleSessionEnd()` | 在 `gateway_stop` 中处理 | `provider.post_session_sync()` → POST `/session/end` |
| `initialize()` | `register()` 中 `await core.initialize()` | `TdaiGateway.start()` 中 `await core.initialize()` |
| `destroy()` | `api.on("gateway_stop")` → `await core.destroy()` | `TdaiGateway.stop()` → `await core.destroy()` |
| `getLogger()` | 返回 `api.logger`（OpenClaw 的 Logger 实例） | 自建 `ConsoleLogger`（用 `console.debug/info/warn/error`） |
| `getLLMRunnerFactory()` | `OpenClawLLMRunnerFactory` → 复用 `api.runtime.agent.runEmbeddedPiAgent()` | `StandaloneLLMRunnerFactory` → 用 Vercel AI SDK 直连 OpenAI API |
